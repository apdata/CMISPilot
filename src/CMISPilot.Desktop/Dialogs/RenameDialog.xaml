<Window x:Class="CMISPilot.Desktop.Dialogs.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Umbenennen"
        Width="400" MinWidth="320" MinHeight="150"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Foreground="{DynamicResource AppTextPrimaryBrush}"
        Background="{DynamicResource AppSurfaceBrush}">

    <!-- Schlanker Ersatz fuer das zunaechst versuchte Inline-Umbenennen der
         DataGrid-Zelle (siehe CLAUDE.md, Fallstricke): der Bearbeitungsmodus
         der Zelle liess sich nicht zuverlaessig wieder verlassen. -->

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Neuer Name:" Margin="0,0,0,6"
                   Foreground="{DynamicResource AppTextPrimaryBrush}" />
        <TextBox Grid.Row="1" x:Name="NameTextBox" Margin="0,0,0,20"
                  Foreground="{DynamicResource AppTextPrimaryBrush}" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Abbrechen" Margin="0,0,8,0" Padding="14,4"
                    IsCancel="True" Click="OnCancelClick"
                    Style="{DynamicResource AppSecondaryButtonStyle}" />
            <Button x:Name="RenameButton" Content="Umbenennen" Padding="14,4"
                    IsDefault="True" Click="OnRenameClick"
                    Style="{DynamicResource AppPrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>
